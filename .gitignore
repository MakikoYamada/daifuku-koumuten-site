# dependencies
node_modules/
**/node_modules/

# build output
dist/
**/dist/

# env (.env.example はコミットする)
.env
.env.*
!.env.example

# lockfile はこのリポジトリでは pnpm-lock.yaml に統一 (npm の lock は混在させない)
package-lock.json

# Claude Code のローカル個人設定 (共有用 .claude/settings.json はコミット可)
.claude/settings.local.json

# misc
.DS_Store
*.log
